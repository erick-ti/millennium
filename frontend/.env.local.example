# URL that the Next.js /api/* rewrite proxy forwards to.
# Host dev (default):    http://localhost:8000
# Docker compose:        http://backend:8000  (service name; set in compose env)
BACKEND_URL=http://localhost:8000
