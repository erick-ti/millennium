// This file is auto-generated by @hey-api/openapi-ts

import { type DefaultError, type InfiniteData, infiniteQueryOptions, queryOptions, type UseMutationOptions } from '@tanstack/react-query';

import { client } from '../client.gen';
import { alertsEventsList, alertsRulesCreate, alertsRulesList, authLoginCreate, authLogoutCreate, authMeRetrieve, cardsCardsArchetypesRetrieve, cardsCardsList, cardsCardsRetrieve, cardsPrintingsList, cardsPrintingsRetrieve, collectionItemsList, collectionItemsRetrieve, collectionLotsList, collectionLotsRetrieve, csrfRetrieve, healthRetrieve, importsBatchesCreate, importsBatchesList, importsBatchesRetrieve, importsRowsApproveCreate, importsRowsList, importsRowsOverrideCreate, importsRowsRejectCreate, importsRowsRetrieve, type Options, portfolioPortfoliosList, portfolioPortfoliosRetrieve, portfolioSnapshotsList, portfolioSnapshotsRetrieve, pricingSnapshotsLatestRetrieve, pricingSnapshotsList, pricingSnapshotsRetrieve, valuationMoversList } from '../sdk.gen';
import type { AlertsEventsListData, AlertsEventsListResponse, AlertsRulesCreateData, AlertsRulesCreateError, AlertsRulesCreateResponse, AlertsRulesListData, AlertsRulesListResponse, AuthLoginCreateData, AuthLoginCreateResponse, AuthLogoutCreateData, AuthMeRetrieveData, AuthMeRetrieveResponse, CardsCardsArchetypesRetrieveData, CardsCardsArchetypesRetrieveResponse, CardsCardsListData, CardsCardsListResponse, CardsCardsRetrieveData, CardsCardsRetrieveResponse, CardsPrintingsListData, CardsPrintingsListResponse, CardsPrintingsRetrieveData, CardsPrintingsRetrieveResponse, CollectionItemsListData, CollectionItemsListResponse, CollectionItemsRetrieveData, CollectionItemsRetrieveResponse, CollectionLotsListData, CollectionLotsListResponse, CollectionLotsRetrieveData, CollectionLotsRetrieveResponse, CsrfRetrieveData, HealthRetrieveData, ImportsBatchesCreateData, ImportsBatchesCreateError, ImportsBatchesCreateResponse, ImportsBatchesListData, ImportsBatchesListResponse, ImportsBatchesRetrieveData, ImportsBatchesRetrieveResponse, ImportsRowsApproveCreateData, ImportsRowsApproveCreateError, ImportsRowsApproveCreateResponse, ImportsRowsListData, ImportsRowsListResponse, ImportsRowsOverrideCreateData, ImportsRowsOverrideCreateError, ImportsRowsOverrideCreateResponse, ImportsRowsRejectCreateData, ImportsRowsRejectCreateError, ImportsRowsRejectCreateResponse, ImportsRowsRetrieveData, ImportsRowsRetrieveResponse, PortfolioPortfoliosListData, PortfolioPortfoliosListResponse, PortfolioPortfoliosRetrieveData, PortfolioPortfoliosRetrieveResponse, PortfolioSnapshotsListData, PortfolioSnapshotsListResponse, PortfolioSnapshotsRetrieveData, PortfolioSnapshotsRetrieveResponse, PricingSnapshotsLatestRetrieveData, PricingSnapshotsLatestRetrieveError, PricingSnapshotsLatestRetrieveResponse, PricingSnapshotsListData, PricingSnapshotsListResponse, PricingSnapshotsRetrieveData, PricingSnapshotsRetrieveResponse, ValuationMoversListData, ValuationMoversListResponse } from '../types.gen';

export type QueryKey<TOptions extends Options> = [
    Pick<TOptions, 'baseUrl' | 'body' | 'headers' | 'path' | 'query'> & {
        _id: string;
        _infinite?: boolean;
        tags?: ReadonlyArray<string>;
    }
];

const createQueryKey = <TOptions extends Options>(id: string, options?: TOptions, infinite?: boolean, tags?: ReadonlyArray<string>): [
    QueryKey<TOptions>[0]
] => {
    const params: QueryKey<TOptions>[0] = { _id: id, baseUrl: options?.baseUrl || (options?.client ?? client).getConfig().baseUrl } as QueryKey<TOptions>[0];
    if (infinite) {
        params._infinite = infinite;
    }
    if (tags) {
        params.tags = tags;
    }
    if (options?.body) {
        params.body = options.body;
    }
    if (options?.headers) {
        params.headers = options.headers;
    }
    if (options?.path) {
        params.path = options.path;
    }
    if (options?.query) {
        params.query = options.query;
    }
    return [params];
};

export const alertsEventsListQueryKey = (options?: Options<AlertsEventsListData>) => createQueryKey('alertsEventsList', options);

/**
 * The price-alert feed (newest first)
 *
 * The in-app alert feed: append-only ``AlertEvent`` rows recorded by the daily
 * evaluation, newest first. LIST-ONLY (a feed, like ``MoversViewSet`` — there is no
 * per-event detail view, so no retrieve endpoint is exposed) and read-only (events are
 * written only by ``run_alerts``). Inherits ``IsAuthenticated`` + ``PageNumberPagination``.
 */
export const alertsEventsListOptions = (options?: Options<AlertsEventsListData>) => queryOptions<AlertsEventsListResponse, DefaultError, AlertsEventsListResponse, ReturnType<typeof alertsEventsListQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await alertsEventsList({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: alertsEventsListQueryKey(options)
});

const createInfiniteParams = <K extends Pick<QueryKey<Options>[0], 'body' | 'headers' | 'path' | 'query'>>(queryKey: QueryKey<Options>, page: K) => {
    const params = { ...queryKey[0] };
    if (page.body) {
        params.body = {
            ...queryKey[0].body as any,
            ...page.body as any
        };
    }
    if (page.headers) {
        params.headers = {
            ...queryKey[0].headers,
            ...page.headers
        };
    }
    if (page.path) {
        params.path = {
            ...queryKey[0].path as any,
            ...page.path as any
        };
    }
    if (page.query) {
        params.query = {
            ...queryKey[0].query as any,
            ...page.query as any
        };
    }
    return params as unknown as typeof page;
};

export const alertsEventsListInfiniteQueryKey = (options?: Options<AlertsEventsListData>): QueryKey<Options<AlertsEventsListData>> => createQueryKey('alertsEventsList', options, true);

/**
 * The price-alert feed (newest first)
 *
 * The in-app alert feed: append-only ``AlertEvent`` rows recorded by the daily
 * evaluation, newest first. LIST-ONLY (a feed, like ``MoversViewSet`` — there is no
 * per-event detail view, so no retrieve endpoint is exposed) and read-only (events are
 * written only by ``run_alerts``). Inherits ``IsAuthenticated`` + ``PageNumberPagination``.
 */
export const alertsEventsListInfiniteOptions = (options?: Options<AlertsEventsListData>) => infiniteQueryOptions<AlertsEventsListResponse, DefaultError, InfiniteData<AlertsEventsListResponse>, QueryKey<Options<AlertsEventsListData>>, number | Pick<QueryKey<Options<AlertsEventsListData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
// @ts-ignore
{
    queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<QueryKey<Options<AlertsEventsListData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
            query: {
                page: pageParam
            }
        };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await alertsEventsList({
            ...options,
            ...params,
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: alertsEventsListInfiniteQueryKey(options)
});

export const alertsRulesListQueryKey = (options?: Options<AlertsRulesListData>) => createQueryKey('alertsRulesList', options);

/**
 * List price-alert rules
 *
 * List + create price-alert rules. Create is the minimal write surface for this slice
 * (edit/delete/mute UI deferred); the global CSRF + session auth apply (``proxy.ts``
 * injects ``X-CSRFToken``). Inherits ``IsAuthenticated`` + ``PageNumberPagination``.
 */
export const alertsRulesListOptions = (options?: Options<AlertsRulesListData>) => queryOptions<AlertsRulesListResponse, DefaultError, AlertsRulesListResponse, ReturnType<typeof alertsRulesListQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await alertsRulesList({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: alertsRulesListQueryKey(options)
});

export const alertsRulesListInfiniteQueryKey = (options?: Options<AlertsRulesListData>): QueryKey<Options<AlertsRulesListData>> => createQueryKey('alertsRulesList', options, true);

/**
 * List price-alert rules
 *
 * List + create price-alert rules. Create is the minimal write surface for this slice
 * (edit/delete/mute UI deferred); the global CSRF + session auth apply (``proxy.ts``
 * injects ``X-CSRFToken``). Inherits ``IsAuthenticated`` + ``PageNumberPagination``.
 */
export const alertsRulesListInfiniteOptions = (options?: Options<AlertsRulesListData>) => infiniteQueryOptions<AlertsRulesListResponse, DefaultError, InfiniteData<AlertsRulesListResponse>, QueryKey<Options<AlertsRulesListData>>, number | Pick<QueryKey<Options<AlertsRulesListData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
// @ts-ignore
{
    queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<QueryKey<Options<AlertsRulesListData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
            query: {
                page: pageParam
            }
        };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await alertsRulesList({
            ...options,
            ...params,
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: alertsRulesListInfiniteQueryKey(options)
});

/**
 * Create a price-alert rule
 *
 * List + create price-alert rules. Create is the minimal write surface for this slice
 * (edit/delete/mute UI deferred); the global CSRF + session auth apply (``proxy.ts``
 * injects ``X-CSRFToken``). Inherits ``IsAuthenticated`` + ``PageNumberPagination``.
 */
export const alertsRulesCreateMutation = (options?: Partial<Options<AlertsRulesCreateData>>): UseMutationOptions<AlertsRulesCreateResponse, AlertsRulesCreateError, Options<AlertsRulesCreateData>> => {
    const mutationOptions: UseMutationOptions<AlertsRulesCreateResponse, AlertsRulesCreateError, Options<AlertsRulesCreateData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await alertsRulesCreate({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Log in (establish a session cookie)
 *
 * Establish a session for valid credentials (Phase 5 auth slice).
 *
 * ``AllowAny`` + no authenticators so an anonymous browser can reach it (the
 * ``HealthView``/``CsrfView`` precedent — every other endpoint stays
 * ``IsAuthenticated``). The credential check + status choice live in
 * ``LoginSerializer`` (a failure is a generic 400, see its docstring).
 *
 * ``csrf_protect`` re-arms CSRF on this POST: DRF marks every ``APIView``
 * ``csrf_exempt`` because CSRF normally runs inside
 * ``SessionAuthentication.enforce_csrf`` — which an *anonymous* request never
 * reaches (it returns before the check). So without this decorator the login
 * POST would be silently CSRF-naked. The ``csrftoken`` is already seeded by
 * ``GET /api/csrf/`` on app load and echoed via ``proxy.ts``'s ``X-CSRFToken``
 * (slice 6), so this composes with zero new frontend plumbing.
 */
export const authLoginCreateMutation = (options?: Partial<Options<AuthLoginCreateData>>): UseMutationOptions<AuthLoginCreateResponse, DefaultError, Options<AuthLoginCreateData>> => {
    const mutationOptions: UseMutationOptions<AuthLoginCreateResponse, DefaultError, Options<AuthLoginCreateData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await authLoginCreate({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Log out (clear the session)
 *
 * Clear the session (Phase 5 auth slice).
 *
 * POST (an unsafe method) deliberately, so it travels the *authenticated* CSRF
 * path: the caller is authenticated, so ``SessionAuthentication.enforce_csrf``
 * runs and ``proxy.ts`` already injects ``X-CSRFToken`` — no ``csrf_protect``
 * needed here (unlike login). Inherits the global ``IsAuthenticated``, so an
 * anonymous logout 403s like everything else. Returns 200 with a body (not 204)
 * so the generated TS client has a typed, non-void response to branch on.
 */
export const authLogoutCreateMutation = (options?: Partial<Options<AuthLogoutCreateData>>): UseMutationOptions<unknown, DefaultError, Options<AuthLogoutCreateData>> => {
    const mutationOptions: UseMutationOptions<unknown, DefaultError, Options<AuthLogoutCreateData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await authLogoutCreate({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const authMeRetrieveQueryKey = (options?: Options<AuthMeRetrieveData>) => createQueryKey('authMeRetrieve', options);

/**
 * Current authenticated user
 *
 * The current authenticated user (Phase 5 auth slice).
 *
 * Inherits the global ``IsAuthenticated``, so an anonymous request → **403**
 * (DRF's session-auth posture: ``authenticate_header`` is ``None``, so a 401
 * downgrades to 403). The SPA's ``AuthProvider`` reads that 403 as "not signed
 * in" — it is the expected anonymous signal, not an error to surface.
 */
export const authMeRetrieveOptions = (options?: Options<AuthMeRetrieveData>) => queryOptions<AuthMeRetrieveResponse, DefaultError, AuthMeRetrieveResponse, ReturnType<typeof authMeRetrieveQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await authMeRetrieve({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: authMeRetrieveQueryKey(options)
});

export const cardsCardsListQueryKey = (options?: Options<CardsCardsListData>) => createQueryKey('cardsCardsList', options);

/**
 * List / search cards
 *
 * Read-only catalog of card identities. List returns ``{id, passcode, name}``
 * and is ``?search=``-filterable by name (the slice-6 import-review override picker
 * finds a card by name → lists its printings); retrieve nests printings (a card has
 * at most a handful — DECISIONS 2026-05-18) so slice 4's card-detail view loads in
 * one round-trip.
 */
export const cardsCardsListOptions = (options?: Options<CardsCardsListData>) => queryOptions<CardsCardsListResponse, DefaultError, CardsCardsListResponse, ReturnType<typeof cardsCardsListQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await cardsCardsList({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: cardsCardsListQueryKey(options)
});

export const cardsCardsListInfiniteQueryKey = (options?: Options<CardsCardsListData>): QueryKey<Options<CardsCardsListData>> => createQueryKey('cardsCardsList', options, true);

/**
 * List / search cards
 *
 * Read-only catalog of card identities. List returns ``{id, passcode, name}``
 * and is ``?search=``-filterable by name (the slice-6 import-review override picker
 * finds a card by name → lists its printings); retrieve nests printings (a card has
 * at most a handful — DECISIONS 2026-05-18) so slice 4's card-detail view loads in
 * one round-trip.
 */
export const cardsCardsListInfiniteOptions = (options?: Options<CardsCardsListData>) => infiniteQueryOptions<CardsCardsListResponse, DefaultError, InfiniteData<CardsCardsListResponse>, QueryKey<Options<CardsCardsListData>>, number | Pick<QueryKey<Options<CardsCardsListData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
// @ts-ignore
{
    queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<QueryKey<Options<CardsCardsListData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
            query: {
                page: pageParam
            }
        };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await cardsCardsList({
            ...options,
            ...params,
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: cardsCardsListInfiniteQueryKey(options)
});

export const cardsCardsRetrieveQueryKey = (options: Options<CardsCardsRetrieveData>) => createQueryKey('cardsCardsRetrieve', options);

/**
 * Retrieve one card (with printings inline)
 *
 * Read-only catalog of card identities. List returns ``{id, passcode, name}``
 * and is ``?search=``-filterable by name (the slice-6 import-review override picker
 * finds a card by name → lists its printings); retrieve nests printings (a card has
 * at most a handful — DECISIONS 2026-05-18) so slice 4's card-detail view loads in
 * one round-trip.
 */
export const cardsCardsRetrieveOptions = (options: Options<CardsCardsRetrieveData>) => queryOptions<CardsCardsRetrieveResponse, DefaultError, CardsCardsRetrieveResponse, ReturnType<typeof cardsCardsRetrieveQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await cardsCardsRetrieve({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: cardsCardsRetrieveQueryKey(options)
});

export const cardsCardsArchetypesRetrieveQueryKey = (options?: Options<CardsCardsArchetypesRetrieveData>) => createQueryKey('cardsCardsArchetypesRetrieve', options);

/**
 * List distinct archetypes
 *
 * Every distinct non-null archetype, sorted — the source for the /cards archetype filter dropdown. Not paginated (a few hundred at most).
 */
export const cardsCardsArchetypesRetrieveOptions = (options?: Options<CardsCardsArchetypesRetrieveData>) => queryOptions<CardsCardsArchetypesRetrieveResponse, DefaultError, CardsCardsArchetypesRetrieveResponse, ReturnType<typeof cardsCardsArchetypesRetrieveQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await cardsCardsArchetypesRetrieve({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: cardsCardsArchetypesRetrieveQueryKey(options)
});

export const cardsPrintingsListQueryKey = (options?: Options<CardsPrintingsListData>) => createQueryKey('cardsPrintingsList', options);

/**
 * List / filter printings
 *
 * Read-only catalog of printings. List filterable by ``?card=`` / ``?set_code=``;
 * global ``PageNumberPagination(PAGE_SIZE=100)`` paginates the ~14k-row catalog.
 */
export const cardsPrintingsListOptions = (options?: Options<CardsPrintingsListData>) => queryOptions<CardsPrintingsListResponse, DefaultError, CardsPrintingsListResponse, ReturnType<typeof cardsPrintingsListQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await cardsPrintingsList({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: cardsPrintingsListQueryKey(options)
});

export const cardsPrintingsListInfiniteQueryKey = (options?: Options<CardsPrintingsListData>): QueryKey<Options<CardsPrintingsListData>> => createQueryKey('cardsPrintingsList', options, true);

/**
 * List / filter printings
 *
 * Read-only catalog of printings. List filterable by ``?card=`` / ``?set_code=``;
 * global ``PageNumberPagination(PAGE_SIZE=100)`` paginates the ~14k-row catalog.
 */
export const cardsPrintingsListInfiniteOptions = (options?: Options<CardsPrintingsListData>) => infiniteQueryOptions<CardsPrintingsListResponse, DefaultError, InfiniteData<CardsPrintingsListResponse>, QueryKey<Options<CardsPrintingsListData>>, number | Pick<QueryKey<Options<CardsPrintingsListData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
// @ts-ignore
{
    queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<QueryKey<Options<CardsPrintingsListData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
            query: {
                page: pageParam
            }
        };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await cardsPrintingsList({
            ...options,
            ...params,
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: cardsPrintingsListInfiniteQueryKey(options)
});

export const cardsPrintingsRetrieveQueryKey = (options: Options<CardsPrintingsRetrieveData>) => createQueryKey('cardsPrintingsRetrieve', options);

/**
 * Retrieve one printing
 *
 * Read-only catalog of printings. List filterable by ``?card=`` / ``?set_code=``;
 * global ``PageNumberPagination(PAGE_SIZE=100)`` paginates the ~14k-row catalog.
 */
export const cardsPrintingsRetrieveOptions = (options: Options<CardsPrintingsRetrieveData>) => queryOptions<CardsPrintingsRetrieveResponse, DefaultError, CardsPrintingsRetrieveResponse, ReturnType<typeof cardsPrintingsRetrieveQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await cardsPrintingsRetrieve({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: cardsPrintingsRetrieveQueryKey(options)
});

export const collectionItemsListQueryKey = (options?: Options<CollectionItemsListData>) => createQueryKey('collectionItemsList', options);

/**
 * List / filter collection items (with aggregate quantity)
 *
 * Read-only collection holdings. List returns one row per holding with
 * ``quantity`` = SUM over lots (an item with no lots reads as 0); retrieve
 * nests the lots — the per-acquisition cost-basis history. ``portfolio`` /
 * ``printing`` FKs and the storage location are pre-joined for the list shape.
 */
export const collectionItemsListOptions = (options?: Options<CollectionItemsListData>) => queryOptions<CollectionItemsListResponse, DefaultError, CollectionItemsListResponse, ReturnType<typeof collectionItemsListQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await collectionItemsList({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: collectionItemsListQueryKey(options)
});

export const collectionItemsListInfiniteQueryKey = (options?: Options<CollectionItemsListData>): QueryKey<Options<CollectionItemsListData>> => createQueryKey('collectionItemsList', options, true);

/**
 * List / filter collection items (with aggregate quantity)
 *
 * Read-only collection holdings. List returns one row per holding with
 * ``quantity`` = SUM over lots (an item with no lots reads as 0); retrieve
 * nests the lots — the per-acquisition cost-basis history. ``portfolio`` /
 * ``printing`` FKs and the storage location are pre-joined for the list shape.
 */
export const collectionItemsListInfiniteOptions = (options?: Options<CollectionItemsListData>) => infiniteQueryOptions<CollectionItemsListResponse, DefaultError, InfiniteData<CollectionItemsListResponse>, QueryKey<Options<CollectionItemsListData>>, number | Pick<QueryKey<Options<CollectionItemsListData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
// @ts-ignore
{
    queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<QueryKey<Options<CollectionItemsListData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
            query: {
                page: pageParam
            }
        };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await collectionItemsList({
            ...options,
            ...params,
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: collectionItemsListInfiniteQueryKey(options)
});

export const collectionItemsRetrieveQueryKey = (options: Options<CollectionItemsRetrieveData>) => createQueryKey('collectionItemsRetrieve', options);

/**
 * Retrieve one holding (with lots inline)
 *
 * Read-only collection holdings. List returns one row per holding with
 * ``quantity`` = SUM over lots (an item with no lots reads as 0); retrieve
 * nests the lots — the per-acquisition cost-basis history. ``portfolio`` /
 * ``printing`` FKs and the storage location are pre-joined for the list shape.
 */
export const collectionItemsRetrieveOptions = (options: Options<CollectionItemsRetrieveData>) => queryOptions<CollectionItemsRetrieveResponse, DefaultError, CollectionItemsRetrieveResponse, ReturnType<typeof collectionItemsRetrieveQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await collectionItemsRetrieve({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: collectionItemsRetrieveQueryKey(options)
});

export const collectionLotsListQueryKey = (options?: Options<CollectionLotsListData>) => createQueryKey('collectionLotsList', options);

/**
 * List / filter collection lots
 *
 * Read-only per-acquisition lots. List filterable by ``?item=`` /
 * ``?portfolio=`` (via the item join). Default order matches the model's
 * natural (item, acquired_at-asc-nulls-last, id) — chronological within a
 * holding, with unknown-date lots last.
 */
export const collectionLotsListOptions = (options?: Options<CollectionLotsListData>) => queryOptions<CollectionLotsListResponse, DefaultError, CollectionLotsListResponse, ReturnType<typeof collectionLotsListQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await collectionLotsList({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: collectionLotsListQueryKey(options)
});

export const collectionLotsListInfiniteQueryKey = (options?: Options<CollectionLotsListData>): QueryKey<Options<CollectionLotsListData>> => createQueryKey('collectionLotsList', options, true);

/**
 * List / filter collection lots
 *
 * Read-only per-acquisition lots. List filterable by ``?item=`` /
 * ``?portfolio=`` (via the item join). Default order matches the model's
 * natural (item, acquired_at-asc-nulls-last, id) — chronological within a
 * holding, with unknown-date lots last.
 */
export const collectionLotsListInfiniteOptions = (options?: Options<CollectionLotsListData>) => infiniteQueryOptions<CollectionLotsListResponse, DefaultError, InfiniteData<CollectionLotsListResponse>, QueryKey<Options<CollectionLotsListData>>, number | Pick<QueryKey<Options<CollectionLotsListData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
// @ts-ignore
{
    queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<QueryKey<Options<CollectionLotsListData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
            query: {
                page: pageParam
            }
        };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await collectionLotsList({
            ...options,
            ...params,
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: collectionLotsListInfiniteQueryKey(options)
});

export const collectionLotsRetrieveQueryKey = (options: Options<CollectionLotsRetrieveData>) => createQueryKey('collectionLotsRetrieve', options);

/**
 * Retrieve one lot
 *
 * Read-only per-acquisition lots. List filterable by ``?item=`` /
 * ``?portfolio=`` (via the item join). Default order matches the model's
 * natural (item, acquired_at-asc-nulls-last, id) — chronological within a
 * holding, with unknown-date lots last.
 */
export const collectionLotsRetrieveOptions = (options: Options<CollectionLotsRetrieveData>) => queryOptions<CollectionLotsRetrieveResponse, DefaultError, CollectionLotsRetrieveResponse, ReturnType<typeof collectionLotsRetrieveQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await collectionLotsRetrieve({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: collectionLotsRetrieveQueryKey(options)
});

export const csrfRetrieveQueryKey = (options?: Options<CsrfRetrieveData>) => createQueryKey('csrfRetrieve', options);

/**
 * Seed the CSRF cookie
 *
 * Seed the ``csrftoken`` cookie (slice 6, DECISIONS 2026-05-29).
 *
 * Django sets the cookie only when a request *uses* the token (``get_token``);
 * with ``CSRF_USE_SESSIONS=False`` and an all-JSON API that never renders a
 * form, nothing here was setting it — so the SPA had no token to send on its
 * first POST. The frontend GETs this on load; ``CsrfViewMiddleware`` then writes
 * the (non-HttpOnly) cookie, and ``proxy.ts`` copies it into ``X-CSRFToken`` on
 * unsafe requests. ``AllowAny`` + no auth: a not-yet-signed-in browser must be
 * able to seed the cookie, and a CSRF cookie leaks nothing (it's a per-session
 * secret the client already holds). Safe method → no CSRF *enforcement* here.
 */
export const csrfRetrieveOptions = (options?: Options<CsrfRetrieveData>) => queryOptions<unknown, DefaultError, unknown, ReturnType<typeof csrfRetrieveQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await csrfRetrieve({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: csrfRetrieveQueryKey(options)
});

export const healthRetrieveQueryKey = (options?: Options<HealthRetrieveData>) => createQueryKey('healthRetrieve', options);

/**
 * Service health
 *
 * Liveness probe — returns 200 when the process is up.
 */
export const healthRetrieveOptions = (options?: Options<HealthRetrieveData>) => queryOptions<unknown, DefaultError, unknown, ReturnType<typeof healthRetrieveQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await healthRetrieve({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: healthRetrieveQueryKey(options)
});

export const importsBatchesListQueryKey = (options?: Options<ImportsBatchesListData>) => createQueryKey('importsBatchesList', options);

/**
 * List import batches with per-status row counts
 *
 * Import history + upload. List/retrieve batches with derived per-status row counts, and
 * POST a Dragon Shield CSV to ``/api/imports/batches/`` to run an import (slice 6). The review
 * surface acts on a batch's *rows* (``ImportRowViewSet``), never on batches directly. Defining
 * ``create`` makes the router bind POST on the collection route (no separate mixin needed).
 */
export const importsBatchesListOptions = (options?: Options<ImportsBatchesListData>) => queryOptions<ImportsBatchesListResponse, DefaultError, ImportsBatchesListResponse, ReturnType<typeof importsBatchesListQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await importsBatchesList({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: importsBatchesListQueryKey(options)
});

export const importsBatchesListInfiniteQueryKey = (options?: Options<ImportsBatchesListData>): QueryKey<Options<ImportsBatchesListData>> => createQueryKey('importsBatchesList', options, true);

/**
 * List import batches with per-status row counts
 *
 * Import history + upload. List/retrieve batches with derived per-status row counts, and
 * POST a Dragon Shield CSV to ``/api/imports/batches/`` to run an import (slice 6). The review
 * surface acts on a batch's *rows* (``ImportRowViewSet``), never on batches directly. Defining
 * ``create`` makes the router bind POST on the collection route (no separate mixin needed).
 */
export const importsBatchesListInfiniteOptions = (options?: Options<ImportsBatchesListData>) => infiniteQueryOptions<ImportsBatchesListResponse, DefaultError, InfiniteData<ImportsBatchesListResponse>, QueryKey<Options<ImportsBatchesListData>>, number | Pick<QueryKey<Options<ImportsBatchesListData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
// @ts-ignore
{
    queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<QueryKey<Options<ImportsBatchesListData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
            query: {
                page: pageParam
            }
        };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await importsBatchesList({
            ...options,
            ...params,
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: importsBatchesListInfiniteQueryKey(options)
});

/**
 * Upload a Dragon Shield CSV → run the import
 *
 * Upload a Dragon Shield CSV and run the import synchronously (DECISIONS 2026-05-29).
 *
 * Decode the file as utf-8-sig (mirroring the ``import_dragon_shield`` command — Excel
 * "CSV UTF-8" saves prepend a BOM), hand the text to ``run_import``, and return the created
 * batch with its derived row counts. A file that isn't a recognized DS export is **not** a
 * request error: ``run_import`` records a FAILED ``ImportBatch`` (a durable history row), so
 * this returns **201 with status=failed** and the frontend branches on ``batch.status``.
 * Only a missing / oversized / non-text file is a **400**. Synchronous (not Celery) is fine
 * at single-user few-thousand-row scale; ``MAX_UPLOAD_BYTES`` bounds the request time, and
 * ``run_import`` returns a JSON-native result so a future Celery promotion is a drop-in.
 */
export const importsBatchesCreateMutation = (options?: Partial<Options<ImportsBatchesCreateData>>): UseMutationOptions<ImportsBatchesCreateResponse, ImportsBatchesCreateError, Options<ImportsBatchesCreateData>> => {
    const mutationOptions: UseMutationOptions<ImportsBatchesCreateResponse, ImportsBatchesCreateError, Options<ImportsBatchesCreateData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await importsBatchesCreate({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const importsBatchesRetrieveQueryKey = (options: Options<ImportsBatchesRetrieveData>) => createQueryKey('importsBatchesRetrieve', options);

/**
 * Retrieve one import batch
 *
 * Import history + upload. List/retrieve batches with derived per-status row counts, and
 * POST a Dragon Shield CSV to ``/api/imports/batches/`` to run an import (slice 6). The review
 * surface acts on a batch's *rows* (``ImportRowViewSet``), never on batches directly. Defining
 * ``create`` makes the router bind POST on the collection route (no separate mixin needed).
 */
export const importsBatchesRetrieveOptions = (options: Options<ImportsBatchesRetrieveData>) => queryOptions<ImportsBatchesRetrieveResponse, DefaultError, ImportsBatchesRetrieveResponse, ReturnType<typeof importsBatchesRetrieveQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await importsBatchesRetrieve({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: importsBatchesRetrieveQueryKey(options)
});

export const importsRowsListQueryKey = (options?: Options<ImportsRowsListData>) => createQueryKey('importsRowsList', options);

/**
 * List / filter import rows
 *
 * The review queue. List/filter staged rows (by ``batch`` / ``status`` /
 * ``match_confidence`` / ``needs_review`` — served by the ``(batch, status)`` index) and
 * resolve a PENDING one via three actions, all routed through ``apps.imports.sync`` so the
 * collection writes go through the single ``_materialize`` chokepoint (DECISIONS 2026-05-26
 * slice 4): ``approve`` materializes the row's matched printing, ``override`` re-points it at
 * a human-chosen printing, ``reject`` skips it.
 */
export const importsRowsListOptions = (options?: Options<ImportsRowsListData>) => queryOptions<ImportsRowsListResponse, DefaultError, ImportsRowsListResponse, ReturnType<typeof importsRowsListQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await importsRowsList({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: importsRowsListQueryKey(options)
});

export const importsRowsListInfiniteQueryKey = (options?: Options<ImportsRowsListData>): QueryKey<Options<ImportsRowsListData>> => createQueryKey('importsRowsList', options, true);

/**
 * List / filter import rows
 *
 * The review queue. List/filter staged rows (by ``batch`` / ``status`` /
 * ``match_confidence`` / ``needs_review`` — served by the ``(batch, status)`` index) and
 * resolve a PENDING one via three actions, all routed through ``apps.imports.sync`` so the
 * collection writes go through the single ``_materialize`` chokepoint (DECISIONS 2026-05-26
 * slice 4): ``approve`` materializes the row's matched printing, ``override`` re-points it at
 * a human-chosen printing, ``reject`` skips it.
 */
export const importsRowsListInfiniteOptions = (options?: Options<ImportsRowsListData>) => infiniteQueryOptions<ImportsRowsListResponse, DefaultError, InfiniteData<ImportsRowsListResponse>, QueryKey<Options<ImportsRowsListData>>, number | Pick<QueryKey<Options<ImportsRowsListData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
// @ts-ignore
{
    queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<QueryKey<Options<ImportsRowsListData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
            query: {
                page: pageParam
            }
        };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await importsRowsList({
            ...options,
            ...params,
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: importsRowsListInfiniteQueryKey(options)
});

export const importsRowsRetrieveQueryKey = (options: Options<ImportsRowsRetrieveData>) => createQueryKey('importsRowsRetrieve', options);

/**
 * Retrieve one import row
 *
 * The review queue. List/filter staged rows (by ``batch`` / ``status`` /
 * ``match_confidence`` / ``needs_review`` — served by the ``(batch, status)`` index) and
 * resolve a PENDING one via three actions, all routed through ``apps.imports.sync`` so the
 * collection writes go through the single ``_materialize`` chokepoint (DECISIONS 2026-05-26
 * slice 4): ``approve`` materializes the row's matched printing, ``override`` re-points it at
 * a human-chosen printing, ``reject`` skips it.
 */
export const importsRowsRetrieveOptions = (options: Options<ImportsRowsRetrieveData>) => queryOptions<ImportsRowsRetrieveResponse, DefaultError, ImportsRowsRetrieveResponse, ReturnType<typeof importsRowsRetrieveQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await importsRowsRetrieve({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: importsRowsRetrieveQueryKey(options)
});

/**
 * Approve a row → materialize its matched printing
 *
 * Materialize the row through ``_materialize``, overriding the auto-materialization
 * freshness gate (the reviewer is the human attention that gate proxies for — DECISIONS
 * 2026-05-27). 200 with the updated row on MATERIALIZED/SKIPPED; 409 when the holding was
 * already imported with a different quantity/cost/date (the row stays PENDING — the API
 * surfaces the conflict rather than overwriting cost basis); 400 if the row isn't an
 * approvable PENDING row with a matched printing.
 */
export const importsRowsApproveCreateMutation = (options?: Partial<Options<ImportsRowsApproveCreateData>>): UseMutationOptions<ImportsRowsApproveCreateResponse, ImportsRowsApproveCreateError, Options<ImportsRowsApproveCreateData>> => {
    const mutationOptions: UseMutationOptions<ImportsRowsApproveCreateResponse, ImportsRowsApproveCreateError, Options<ImportsRowsApproveCreateData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await importsRowsApproveCreate({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Override a row's matched printing
 *
 * Re-point the row at a human-chosen ``CardPrinting`` (body: ``{"printing": <id>}``)
 * and leave it PENDING so the corrected match can be approved next. Approvable
 * immediately after (``approve`` keys on a present matched printing, not on confidence).
 */
export const importsRowsOverrideCreateMutation = (options?: Partial<Options<ImportsRowsOverrideCreateData>>): UseMutationOptions<ImportsRowsOverrideCreateResponse, ImportsRowsOverrideCreateError, Options<ImportsRowsOverrideCreateData>> => {
    const mutationOptions: UseMutationOptions<ImportsRowsOverrideCreateResponse, ImportsRowsOverrideCreateError, Options<ImportsRowsOverrideCreateData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await importsRowsOverrideCreate({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Reject a row → skipped
 *
 * Mark a PENDING row SKIPPED (the reviewer declines the holding). 400 if the row is
 * not PENDING (a MATERIALIZED row already wrote a holding; an ERROR row is terminal).
 */
export const importsRowsRejectCreateMutation = (options?: Partial<Options<ImportsRowsRejectCreateData>>): UseMutationOptions<ImportsRowsRejectCreateResponse, ImportsRowsRejectCreateError, Options<ImportsRowsRejectCreateData>> => {
    const mutationOptions: UseMutationOptions<ImportsRowsRejectCreateResponse, ImportsRowsRejectCreateError, Options<ImportsRowsRejectCreateData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await importsRowsRejectCreate({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const portfolioPortfoliosListQueryKey = (options?: Options<PortfolioPortfoliosListData>) => createQueryKey('portfolioPortfoliosList', options);

/**
 * List portfolios (with latest value snapshot inline)
 *
 * Read-only portfolios. Each row carries the latest ``PortfolioValueSnapshot``
 * inline (NULL when a portfolio has never been valued, e.g. a freshly-created
 * one from a Dragon Shield import that runs before the next 04:00 UTC
 * valuation beat — DECISIONS 2026-05-25 slice 4c).
 */
export const portfolioPortfoliosListOptions = (options?: Options<PortfolioPortfoliosListData>) => queryOptions<PortfolioPortfoliosListResponse, DefaultError, PortfolioPortfoliosListResponse, ReturnType<typeof portfolioPortfoliosListQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await portfolioPortfoliosList({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: portfolioPortfoliosListQueryKey(options)
});

export const portfolioPortfoliosListInfiniteQueryKey = (options?: Options<PortfolioPortfoliosListData>): QueryKey<Options<PortfolioPortfoliosListData>> => createQueryKey('portfolioPortfoliosList', options, true);

/**
 * List portfolios (with latest value snapshot inline)
 *
 * Read-only portfolios. Each row carries the latest ``PortfolioValueSnapshot``
 * inline (NULL when a portfolio has never been valued, e.g. a freshly-created
 * one from a Dragon Shield import that runs before the next 04:00 UTC
 * valuation beat — DECISIONS 2026-05-25 slice 4c).
 */
export const portfolioPortfoliosListInfiniteOptions = (options?: Options<PortfolioPortfoliosListData>) => infiniteQueryOptions<PortfolioPortfoliosListResponse, DefaultError, InfiniteData<PortfolioPortfoliosListResponse>, QueryKey<Options<PortfolioPortfoliosListData>>, number | Pick<QueryKey<Options<PortfolioPortfoliosListData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
// @ts-ignore
{
    queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<QueryKey<Options<PortfolioPortfoliosListData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
            query: {
                page: pageParam
            }
        };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await portfolioPortfoliosList({
            ...options,
            ...params,
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: portfolioPortfoliosListInfiniteQueryKey(options)
});

export const portfolioPortfoliosRetrieveQueryKey = (options: Options<PortfolioPortfoliosRetrieveData>) => createQueryKey('portfolioPortfoliosRetrieve', options);

/**
 * Retrieve one portfolio (with latest snapshot inline)
 *
 * Read-only portfolios. Each row carries the latest ``PortfolioValueSnapshot``
 * inline (NULL when a portfolio has never been valued, e.g. a freshly-created
 * one from a Dragon Shield import that runs before the next 04:00 UTC
 * valuation beat — DECISIONS 2026-05-25 slice 4c).
 */
export const portfolioPortfoliosRetrieveOptions = (options: Options<PortfolioPortfoliosRetrieveData>) => queryOptions<PortfolioPortfoliosRetrieveResponse, DefaultError, PortfolioPortfoliosRetrieveResponse, ReturnType<typeof portfolioPortfoliosRetrieveQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await portfolioPortfoliosRetrieve({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: portfolioPortfoliosRetrieveQueryKey(options)
});

export const portfolioSnapshotsListQueryKey = (options?: Options<PortfolioSnapshotsListData>) => createQueryKey('portfolioSnapshotsList', options);

/**
 * List / filter portfolio value snapshots (append-only daily history)
 *
 * Read-only append-only daily valuation timeline. The slice-5 portfolio
 * chart consumes ``?portfolio=&from=&to=`` to pull a range, then renders the
 * value series. ``unrealized_gain`` may be NULL on a row (partial coverage —
 * DECISIONS 2026-05-25 slice 4a); consumers handle NULL distinctly from 0.
 */
export const portfolioSnapshotsListOptions = (options?: Options<PortfolioSnapshotsListData>) => queryOptions<PortfolioSnapshotsListResponse, DefaultError, PortfolioSnapshotsListResponse, ReturnType<typeof portfolioSnapshotsListQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await portfolioSnapshotsList({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: portfolioSnapshotsListQueryKey(options)
});

export const portfolioSnapshotsListInfiniteQueryKey = (options?: Options<PortfolioSnapshotsListData>): QueryKey<Options<PortfolioSnapshotsListData>> => createQueryKey('portfolioSnapshotsList', options, true);

/**
 * List / filter portfolio value snapshots (append-only daily history)
 *
 * Read-only append-only daily valuation timeline. The slice-5 portfolio
 * chart consumes ``?portfolio=&from=&to=`` to pull a range, then renders the
 * value series. ``unrealized_gain`` may be NULL on a row (partial coverage —
 * DECISIONS 2026-05-25 slice 4a); consumers handle NULL distinctly from 0.
 */
export const portfolioSnapshotsListInfiniteOptions = (options?: Options<PortfolioSnapshotsListData>) => infiniteQueryOptions<PortfolioSnapshotsListResponse, DefaultError, InfiniteData<PortfolioSnapshotsListResponse>, QueryKey<Options<PortfolioSnapshotsListData>>, number | Pick<QueryKey<Options<PortfolioSnapshotsListData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
// @ts-ignore
{
    queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<QueryKey<Options<PortfolioSnapshotsListData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
            query: {
                page: pageParam
            }
        };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await portfolioSnapshotsList({
            ...options,
            ...params,
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: portfolioSnapshotsListInfiniteQueryKey(options)
});

export const portfolioSnapshotsRetrieveQueryKey = (options: Options<PortfolioSnapshotsRetrieveData>) => createQueryKey('portfolioSnapshotsRetrieve', options);

/**
 * Retrieve one portfolio value snapshot
 *
 * Read-only append-only daily valuation timeline. The slice-5 portfolio
 * chart consumes ``?portfolio=&from=&to=`` to pull a range, then renders the
 * value series. ``unrealized_gain`` may be NULL on a row (partial coverage —
 * DECISIONS 2026-05-25 slice 4a); consumers handle NULL distinctly from 0.
 */
export const portfolioSnapshotsRetrieveOptions = (options: Options<PortfolioSnapshotsRetrieveData>) => queryOptions<PortfolioSnapshotsRetrieveResponse, DefaultError, PortfolioSnapshotsRetrieveResponse, ReturnType<typeof portfolioSnapshotsRetrieveQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await portfolioSnapshotsRetrieve({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: portfolioSnapshotsRetrieveQueryKey(options)
});

export const pricingSnapshotsListQueryKey = (options?: Options<PricingSnapshotsListData>) => createQueryKey('pricingSnapshotsList', options);

/**
 * List / filter price snapshots (append-only daily history)
 *
 * Read-only append-only price history. List filterable by
 * ``?printing=&edition=&from=&to=`` — the slice-4 price-chart shape. The
 * ``latest`` action returns the most-recent snapshot for one
 * ``(printing, edition)``, the "today's price" lookup; this is structured as
 * an action (not the latest-first ordered list's first row) so a consumer
 * can't accidentally page into history when it only wants today.
 */
export const pricingSnapshotsListOptions = (options?: Options<PricingSnapshotsListData>) => queryOptions<PricingSnapshotsListResponse, DefaultError, PricingSnapshotsListResponse, ReturnType<typeof pricingSnapshotsListQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await pricingSnapshotsList({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: pricingSnapshotsListQueryKey(options)
});

export const pricingSnapshotsListInfiniteQueryKey = (options?: Options<PricingSnapshotsListData>): QueryKey<Options<PricingSnapshotsListData>> => createQueryKey('pricingSnapshotsList', options, true);

/**
 * List / filter price snapshots (append-only daily history)
 *
 * Read-only append-only price history. List filterable by
 * ``?printing=&edition=&from=&to=`` — the slice-4 price-chart shape. The
 * ``latest`` action returns the most-recent snapshot for one
 * ``(printing, edition)``, the "today's price" lookup; this is structured as
 * an action (not the latest-first ordered list's first row) so a consumer
 * can't accidentally page into history when it only wants today.
 */
export const pricingSnapshotsListInfiniteOptions = (options?: Options<PricingSnapshotsListData>) => infiniteQueryOptions<PricingSnapshotsListResponse, DefaultError, InfiniteData<PricingSnapshotsListResponse>, QueryKey<Options<PricingSnapshotsListData>>, number | Pick<QueryKey<Options<PricingSnapshotsListData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
// @ts-ignore
{
    queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<QueryKey<Options<PricingSnapshotsListData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
            query: {
                page: pageParam
            }
        };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await pricingSnapshotsList({
            ...options,
            ...params,
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: pricingSnapshotsListInfiniteQueryKey(options)
});

export const pricingSnapshotsRetrieveQueryKey = (options: Options<PricingSnapshotsRetrieveData>) => createQueryKey('pricingSnapshotsRetrieve', options);

/**
 * Retrieve one price snapshot
 *
 * Read-only append-only price history. List filterable by
 * ``?printing=&edition=&from=&to=`` — the slice-4 price-chart shape. The
 * ``latest`` action returns the most-recent snapshot for one
 * ``(printing, edition)``, the "today's price" lookup; this is structured as
 * an action (not the latest-first ordered list's first row) so a consumer
 * can't accidentally page into history when it only wants today.
 */
export const pricingSnapshotsRetrieveOptions = (options: Options<PricingSnapshotsRetrieveData>) => queryOptions<PricingSnapshotsRetrieveResponse, DefaultError, PricingSnapshotsRetrieveResponse, ReturnType<typeof pricingSnapshotsRetrieveQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await pricingSnapshotsRetrieve({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: pricingSnapshotsRetrieveQueryKey(options)
});

export const pricingSnapshotsLatestRetrieveQueryKey = (options: Options<PricingSnapshotsLatestRetrieveData>) => createQueryKey('pricingSnapshotsLatestRetrieve', options);

/**
 * Latest snapshot for one (printing, edition) — the 'today's price' lookup
 *
 * The most recent snapshot for one ``(printing, edition)``. 404 when no
 * snapshot exists for the pair (a printing TCGCSV doesn't price, or one
 * the daily reconcile hasn't run for yet).
 */
export const pricingSnapshotsLatestRetrieveOptions = (options: Options<PricingSnapshotsLatestRetrieveData>) => queryOptions<PricingSnapshotsLatestRetrieveResponse, PricingSnapshotsLatestRetrieveError, PricingSnapshotsLatestRetrieveResponse, ReturnType<typeof pricingSnapshotsLatestRetrieveQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await pricingSnapshotsLatestRetrieve({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: pricingSnapshotsLatestRetrieveQueryKey(options)
});

export const valuationMoversListQueryKey = (options?: Options<ValuationMoversListData>) => createQueryKey('valuationMoversList', options);

/**
 * Biggest price movers among owned (printing, edition) pairs
 *
 * Read-only "biggest movers" analytics (DECISIONS 2026-05-31): each owned
 * ``(printing, edition)``'s price change over a selectable window. Rows are
 * computed from the valuation engine's usable-price helpers across two date
 * anchors (today and today - window), scoped to currently-held holdings, then
 * server-ordered (the ``?ordering=`` allowlist) and paginated — like every other
 * list endpoint. Inherits the global ``IsAuthenticated`` + ``PageNumberPagination``.
 */
export const valuationMoversListOptions = (options?: Options<ValuationMoversListData>) => queryOptions<ValuationMoversListResponse, DefaultError, ValuationMoversListResponse, ReturnType<typeof valuationMoversListQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await valuationMoversList({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: valuationMoversListQueryKey(options)
});

export const valuationMoversListInfiniteQueryKey = (options?: Options<ValuationMoversListData>): QueryKey<Options<ValuationMoversListData>> => createQueryKey('valuationMoversList', options, true);

/**
 * Biggest price movers among owned (printing, edition) pairs
 *
 * Read-only "biggest movers" analytics (DECISIONS 2026-05-31): each owned
 * ``(printing, edition)``'s price change over a selectable window. Rows are
 * computed from the valuation engine's usable-price helpers across two date
 * anchors (today and today - window), scoped to currently-held holdings, then
 * server-ordered (the ``?ordering=`` allowlist) and paginated — like every other
 * list endpoint. Inherits the global ``IsAuthenticated`` + ``PageNumberPagination``.
 */
export const valuationMoversListInfiniteOptions = (options?: Options<ValuationMoversListData>) => infiniteQueryOptions<ValuationMoversListResponse, DefaultError, InfiniteData<ValuationMoversListResponse>, QueryKey<Options<ValuationMoversListData>>, number | Pick<QueryKey<Options<ValuationMoversListData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
// @ts-ignore
{
    queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<QueryKey<Options<ValuationMoversListData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
            query: {
                page: pageParam
            }
        };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await valuationMoversList({
            ...options,
            ...params,
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: valuationMoversListInfiniteQueryKey(options)
});
