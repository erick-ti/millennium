// This file is auto-generated by @hey-api/openapi-ts

import { type Client, formDataBodySerializer, type Options as Options2, type TDataShape } from './client';
import { client } from './client.gen';
import type { AlertsEventsListData, AlertsEventsListResponses, AlertsRulesCreateData, AlertsRulesCreateErrors, AlertsRulesCreateResponses, AlertsRulesListData, AlertsRulesListResponses, AuthLoginCreateData, AuthLoginCreateErrors, AuthLoginCreateResponses, AuthLogoutCreateData, AuthLogoutCreateResponses, AuthMeRetrieveData, AuthMeRetrieveResponses, CardsCardsArchetypesRetrieveData, CardsCardsArchetypesRetrieveResponses, CardsCardsListData, CardsCardsListResponses, CardsCardsRetrieveData, CardsCardsRetrieveResponses, CardsPrintingsListData, CardsPrintingsListResponses, CardsPrintingsRetrieveData, CardsPrintingsRetrieveResponses, CollectionItemsListData, CollectionItemsListResponses, CollectionItemsRetrieveData, CollectionItemsRetrieveResponses, CollectionLotsListData, CollectionLotsListResponses, CollectionLotsRetrieveData, CollectionLotsRetrieveResponses, CsrfRetrieveData, CsrfRetrieveResponses, DecksDecksCreateData, DecksDecksCreateErrors, DecksDecksCreateResponses, DecksDecksDestroyData, DecksDecksDestroyResponses, DecksDecksListData, DecksDecksListResponses, DecksDecksPartialUpdateData, DecksDecksPartialUpdateResponses, DecksDecksRetrieveData, DecksDecksRetrieveResponses, DecksDecksUpdateData, DecksDecksUpdateResponses, DecksMembershipsCreateData, DecksMembershipsCreateErrors, DecksMembershipsCreateResponses, DecksMembershipsDestroyData, DecksMembershipsDestroyResponses, DecksMembershipsListData, DecksMembershipsListResponses, HealthRetrieveData, HealthRetrieveResponses, ImportsBatchesCreateData, ImportsBatchesCreateErrors, ImportsBatchesCreateResponses, ImportsBatchesListData, ImportsBatchesListResponses, ImportsBatchesRetrieveData, ImportsBatchesRetrieveResponses, ImportsRowsApproveCreateData, ImportsRowsApproveCreateErrors, ImportsRowsApproveCreateResponses, ImportsRowsListData, ImportsRowsListResponses, ImportsRowsOverrideCreateData, ImportsRowsOverrideCreateErrors, ImportsRowsOverrideCreateResponses, ImportsRowsRejectCreateData, ImportsRowsRejectCreateErrors, ImportsRowsRejectCreateResponses, ImportsRowsRetrieveData, ImportsRowsRetrieveResponses, PortfolioPortfoliosListData, PortfolioPortfoliosListResponses, PortfolioPortfoliosRetrieveData, PortfolioPortfoliosRetrieveResponses, PortfolioSnapshotsListData, PortfolioSnapshotsListResponses, PortfolioSnapshotsRetrieveData, PortfolioSnapshotsRetrieveResponses, PricingSnapshotsLatestRetrieveData, PricingSnapshotsLatestRetrieveErrors, PricingSnapshotsLatestRetrieveResponses, PricingSnapshotsListData, PricingSnapshotsListResponses, PricingSnapshotsRetrieveData, PricingSnapshotsRetrieveResponses, StatusChecksRetrieveData, StatusChecksRetrieveResponses, StatusOverviewRetrieveData, StatusOverviewRetrieveResponses, ValuationMoversListData, ValuationMoversListResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * The price-alert feed (newest first)
 *
 * The in-app alert feed: append-only ``AlertEvent`` rows recorded by the daily
 * evaluation, newest first. LIST-ONLY (a feed, like ``MoversViewSet`` — there is no
 * per-event detail view, so no retrieve endpoint is exposed) and read-only (events are
 * written only by ``run_alerts``). Inherits ``IsAuthenticated`` + ``PageNumberPagination``.
 */
export const alertsEventsList = <ThrowOnError extends boolean = false>(options?: Options<AlertsEventsListData, ThrowOnError>) => (options?.client ?? client).get<AlertsEventsListResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/alerts/events/',
    ...options
});

/**
 * List price-alert rules
 *
 * List + create price-alert rules. Create is the minimal write surface for this slice
 * (edit/delete/mute UI deferred); the global CSRF + session auth apply (``proxy.ts``
 * injects ``X-CSRFToken``). Inherits ``IsAuthenticated`` + ``PageNumberPagination``.
 */
export const alertsRulesList = <ThrowOnError extends boolean = false>(options?: Options<AlertsRulesListData, ThrowOnError>) => (options?.client ?? client).get<AlertsRulesListResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/alerts/rules/',
    ...options
});

/**
 * Create a price-alert rule
 *
 * List + create price-alert rules. Create is the minimal write surface for this slice
 * (edit/delete/mute UI deferred); the global CSRF + session auth apply (``proxy.ts``
 * injects ``X-CSRFToken``). Inherits ``IsAuthenticated`` + ``PageNumberPagination``.
 */
export const alertsRulesCreate = <ThrowOnError extends boolean = false>(options: Options<AlertsRulesCreateData, ThrowOnError>) => (options.client ?? client).post<AlertsRulesCreateResponses, AlertsRulesCreateErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/alerts/rules/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Log in (establish a session cookie)
 *
 * Establish a session for valid credentials (Phase 5 auth slice).
 *
 * ``AllowAny`` + no authenticators so an anonymous browser can reach it (the
 * ``HealthView``/``CsrfView`` precedent — every other endpoint stays
 * ``IsAuthenticated``). The credential check + status choice live in
 * ``LoginSerializer`` (a failure is a generic 400, see its docstring).
 *
 * ``csrf_protect`` re-arms CSRF on this POST: DRF marks every ``APIView``
 * ``csrf_exempt`` because CSRF normally runs inside
 * ``SessionAuthentication.enforce_csrf`` — which an *anonymous* request never
 * reaches (it returns before the check). So without this decorator the login
 * POST would be silently CSRF-naked. The ``csrftoken`` is already seeded by
 * ``GET /api/csrf/`` on app load and echoed via ``proxy.ts``'s ``X-CSRFToken``
 * (slice 6), so this composes with zero new frontend plumbing.
 */
export const authLoginCreate = <ThrowOnError extends boolean = false>(options: Options<AuthLoginCreateData, ThrowOnError>) => (options.client ?? client).post<AuthLoginCreateResponses, AuthLoginCreateErrors, ThrowOnError>({
    url: '/api/auth/login/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Log out (clear the session)
 *
 * Clear the session (Phase 5 auth slice).
 *
 * POST (an unsafe method) deliberately, so it travels the *authenticated* CSRF
 * path: the caller is authenticated, so ``SessionAuthentication.enforce_csrf``
 * runs and ``proxy.ts`` already injects ``X-CSRFToken`` — no ``csrf_protect``
 * needed here (unlike login). Inherits the global ``IsAuthenticated``, so an
 * anonymous logout 403s like everything else. Returns 200 with a body (not 204)
 * so the generated TS client has a typed, non-void response to branch on.
 */
export const authLogoutCreate = <ThrowOnError extends boolean = false>(options?: Options<AuthLogoutCreateData, ThrowOnError>) => (options?.client ?? client).post<AuthLogoutCreateResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/auth/logout/',
    ...options
});

/**
 * Current authenticated user
 *
 * The current authenticated user (Phase 5 auth slice).
 *
 * Inherits the global ``IsAuthenticated``, so an anonymous request → **403**
 * (DRF's session-auth posture: ``authenticate_header`` is ``None``, so a 401
 * downgrades to 403). The SPA's ``AuthProvider`` reads that 403 as "not signed
 * in" — it is the expected anonymous signal, not an error to surface.
 */
export const authMeRetrieve = <ThrowOnError extends boolean = false>(options?: Options<AuthMeRetrieveData, ThrowOnError>) => (options?.client ?? client).get<AuthMeRetrieveResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/auth/me/',
    ...options
});

/**
 * List / search cards
 *
 * Read-only catalog of card identities. List returns ``{id, passcode, name}``
 * and is ``?search=``-filterable by name (the slice-6 import-review override picker
 * finds a card by name → lists its printings); retrieve nests printings (a card has
 * at most a handful — DECISIONS 2026-05-18) so slice 4's card-detail view loads in
 * one round-trip.
 */
export const cardsCardsList = <ThrowOnError extends boolean = false>(options?: Options<CardsCardsListData, ThrowOnError>) => (options?.client ?? client).get<CardsCardsListResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/cards/cards/',
    ...options
});

/**
 * Retrieve one card (with printings inline)
 *
 * Read-only catalog of card identities. List returns ``{id, passcode, name}``
 * and is ``?search=``-filterable by name (the slice-6 import-review override picker
 * finds a card by name → lists its printings); retrieve nests printings (a card has
 * at most a handful — DECISIONS 2026-05-18) so slice 4's card-detail view loads in
 * one round-trip.
 */
export const cardsCardsRetrieve = <ThrowOnError extends boolean = false>(options: Options<CardsCardsRetrieveData, ThrowOnError>) => (options.client ?? client).get<CardsCardsRetrieveResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/cards/cards/{id}/',
    ...options
});

/**
 * List distinct archetypes
 *
 * Every distinct non-null archetype, sorted — the source for the /cards archetype filter dropdown. Not paginated (a few hundred at most).
 */
export const cardsCardsArchetypesRetrieve = <ThrowOnError extends boolean = false>(options?: Options<CardsCardsArchetypesRetrieveData, ThrowOnError>) => (options?.client ?? client).get<CardsCardsArchetypesRetrieveResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/cards/cards/archetypes/',
    ...options
});

/**
 * List / filter printings
 *
 * Read-only catalog of printings. List filterable by ``?card=`` / ``?set_code=``;
 * global ``PageNumberPagination(PAGE_SIZE=100)`` paginates the ~14k-row catalog.
 */
export const cardsPrintingsList = <ThrowOnError extends boolean = false>(options?: Options<CardsPrintingsListData, ThrowOnError>) => (options?.client ?? client).get<CardsPrintingsListResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/cards/printings/',
    ...options
});

/**
 * Retrieve one printing
 *
 * Read-only catalog of printings. List filterable by ``?card=`` / ``?set_code=``;
 * global ``PageNumberPagination(PAGE_SIZE=100)`` paginates the ~14k-row catalog.
 */
export const cardsPrintingsRetrieve = <ThrowOnError extends boolean = false>(options: Options<CardsPrintingsRetrieveData, ThrowOnError>) => (options.client ?? client).get<CardsPrintingsRetrieveResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/cards/printings/{id}/',
    ...options
});

/**
 * List / filter collection items (with aggregate quantity)
 *
 * Read-only collection holdings. List returns one row per holding with
 * ``quantity`` = SUM over lots (an item with no lots reads as 0); retrieve
 * nests the lots — the per-acquisition cost-basis history. ``portfolio`` /
 * ``printing`` FKs and the storage location are pre-joined for the list shape.
 */
export const collectionItemsList = <ThrowOnError extends boolean = false>(options?: Options<CollectionItemsListData, ThrowOnError>) => (options?.client ?? client).get<CollectionItemsListResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/collection/items/',
    ...options
});

/**
 * Retrieve one holding (with lots inline)
 *
 * Read-only collection holdings. List returns one row per holding with
 * ``quantity`` = SUM over lots (an item with no lots reads as 0); retrieve
 * nests the lots — the per-acquisition cost-basis history. ``portfolio`` /
 * ``printing`` FKs and the storage location are pre-joined for the list shape.
 */
export const collectionItemsRetrieve = <ThrowOnError extends boolean = false>(options: Options<CollectionItemsRetrieveData, ThrowOnError>) => (options.client ?? client).get<CollectionItemsRetrieveResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/collection/items/{id}/',
    ...options
});

/**
 * List / filter collection lots
 *
 * Read-only per-acquisition lots. List filterable by ``?item=`` /
 * ``?portfolio=`` (via the item join). Default order matches the model's
 * natural (item, acquired_at-asc-nulls-last, id) — chronological within a
 * holding, with unknown-date lots last.
 */
export const collectionLotsList = <ThrowOnError extends boolean = false>(options?: Options<CollectionLotsListData, ThrowOnError>) => (options?.client ?? client).get<CollectionLotsListResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/collection/lots/',
    ...options
});

/**
 * Retrieve one lot
 *
 * Read-only per-acquisition lots. List filterable by ``?item=`` /
 * ``?portfolio=`` (via the item join). Default order matches the model's
 * natural (item, acquired_at-asc-nulls-last, id) — chronological within a
 * holding, with unknown-date lots last.
 */
export const collectionLotsRetrieve = <ThrowOnError extends boolean = false>(options: Options<CollectionLotsRetrieveData, ThrowOnError>) => (options.client ?? client).get<CollectionLotsRetrieveResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/collection/lots/{id}/',
    ...options
});

/**
 * Seed the CSRF cookie
 *
 * Seed the ``csrftoken`` cookie (slice 6, DECISIONS 2026-05-29).
 *
 * Django sets the cookie only when a request *uses* the token (``get_token``);
 * with ``CSRF_USE_SESSIONS=False`` and an all-JSON API that never renders a
 * form, nothing here was setting it — so the SPA had no token to send on its
 * first POST. The frontend GETs this on load; ``CsrfViewMiddleware`` then writes
 * the (non-HttpOnly) cookie, and ``proxy.ts`` copies it into ``X-CSRFToken`` on
 * unsafe requests. ``AllowAny`` + no auth: a not-yet-signed-in browser must be
 * able to seed the cookie, and a CSRF cookie leaks nothing (it's a per-session
 * secret the client already holds). Safe method → no CSRF *enforcement* here.
 */
export const csrfRetrieve = <ThrowOnError extends boolean = false>(options?: Options<CsrfRetrieveData, ThrowOnError>) => (options?.client ?? client).get<CsrfRetrieveResponses, unknown, ThrowOnError>({ url: '/api/csrf/', ...options });

/**
 * List decks (each with its member count)
 *
 * Full CRUD for decks — mutable user resources (the ``Portfolio`` posture; the
 * explicit-full-surface case where ``ModelViewSet`` is the honest choice). List +
 * retrieve carry a ``member_count`` annotation; create / rename (PATCH) / delete are the
 * inherited writes (global session auth + ``proxy.ts`` CSRF apply). Members are managed
 * through the separate ``DeckMembershipViewSet`` (a deck's member feed + add/remove), NOT
 * nested here — members are mutable + paginated, so they live on their own flat endpoint
 * (the import-batch-detail header/rows split, not the cards/collection nested-detail
 * shape). Inherits ``IsAuthenticated`` + ``PageNumberPagination``.
 */
export const decksDecksList = <ThrowOnError extends boolean = false>(options?: Options<DecksDecksListData, ThrowOnError>) => (options?.client ?? client).get<DecksDecksListResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/decks/decks/',
    ...options
});

/**
 * Create a deck
 *
 * Full CRUD for decks — mutable user resources (the ``Portfolio`` posture; the
 * explicit-full-surface case where ``ModelViewSet`` is the honest choice). List +
 * retrieve carry a ``member_count`` annotation; create / rename (PATCH) / delete are the
 * inherited writes (global session auth + ``proxy.ts`` CSRF apply). Members are managed
 * through the separate ``DeckMembershipViewSet`` (a deck's member feed + add/remove), NOT
 * nested here — members are mutable + paginated, so they live on their own flat endpoint
 * (the import-batch-detail header/rows split, not the cards/collection nested-detail
 * shape). Inherits ``IsAuthenticated`` + ``PageNumberPagination``.
 */
export const decksDecksCreate = <ThrowOnError extends boolean = false>(options: Options<DecksDecksCreateData, ThrowOnError>) => (options.client ?? client).post<DecksDecksCreateResponses, DecksDecksCreateErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/decks/decks/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a deck (its memberships cascade away)
 *
 * Full CRUD for decks — mutable user resources (the ``Portfolio`` posture; the
 * explicit-full-surface case where ``ModelViewSet`` is the honest choice). List +
 * retrieve carry a ``member_count`` annotation; create / rename (PATCH) / delete are the
 * inherited writes (global session auth + ``proxy.ts`` CSRF apply). Members are managed
 * through the separate ``DeckMembershipViewSet`` (a deck's member feed + add/remove), NOT
 * nested here — members are mutable + paginated, so they live on their own flat endpoint
 * (the import-batch-detail header/rows split, not the cards/collection nested-detail
 * shape). Inherits ``IsAuthenticated`` + ``PageNumberPagination``.
 */
export const decksDecksDestroy = <ThrowOnError extends boolean = false>(options: Options<DecksDecksDestroyData, ThrowOnError>) => (options.client ?? client).delete<DecksDecksDestroyResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/decks/decks/{id}/',
    ...options
});

/**
 * Retrieve a deck
 *
 * Full CRUD for decks — mutable user resources (the ``Portfolio`` posture; the
 * explicit-full-surface case where ``ModelViewSet`` is the honest choice). List +
 * retrieve carry a ``member_count`` annotation; create / rename (PATCH) / delete are the
 * inherited writes (global session auth + ``proxy.ts`` CSRF apply). Members are managed
 * through the separate ``DeckMembershipViewSet`` (a deck's member feed + add/remove), NOT
 * nested here — members are mutable + paginated, so they live on their own flat endpoint
 * (the import-batch-detail header/rows split, not the cards/collection nested-detail
 * shape). Inherits ``IsAuthenticated`` + ``PageNumberPagination``.
 */
export const decksDecksRetrieve = <ThrowOnError extends boolean = false>(options: Options<DecksDecksRetrieveData, ThrowOnError>) => (options.client ?? client).get<DecksDecksRetrieveResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/decks/decks/{id}/',
    ...options
});

/**
 * Rename / edit a deck
 *
 * Full CRUD for decks — mutable user resources (the ``Portfolio`` posture; the
 * explicit-full-surface case where ``ModelViewSet`` is the honest choice). List +
 * retrieve carry a ``member_count`` annotation; create / rename (PATCH) / delete are the
 * inherited writes (global session auth + ``proxy.ts`` CSRF apply). Members are managed
 * through the separate ``DeckMembershipViewSet`` (a deck's member feed + add/remove), NOT
 * nested here — members are mutable + paginated, so they live on their own flat endpoint
 * (the import-batch-detail header/rows split, not the cards/collection nested-detail
 * shape). Inherits ``IsAuthenticated`` + ``PageNumberPagination``.
 */
export const decksDecksPartialUpdate = <ThrowOnError extends boolean = false>(options: Options<DecksDecksPartialUpdateData, ThrowOnError>) => (options.client ?? client).patch<DecksDecksPartialUpdateResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/decks/decks/{id}/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Replace a deck
 *
 * Full CRUD for decks — mutable user resources (the ``Portfolio`` posture; the
 * explicit-full-surface case where ``ModelViewSet`` is the honest choice). List +
 * retrieve carry a ``member_count`` annotation; create / rename (PATCH) / delete are the
 * inherited writes (global session auth + ``proxy.ts`` CSRF apply). Members are managed
 * through the separate ``DeckMembershipViewSet`` (a deck's member feed + add/remove), NOT
 * nested here — members are mutable + paginated, so they live on their own flat endpoint
 * (the import-batch-detail header/rows split, not the cards/collection nested-detail
 * shape). Inherits ``IsAuthenticated`` + ``PageNumberPagination``.
 */
export const decksDecksUpdate = <ThrowOnError extends boolean = false>(options: Options<DecksDecksUpdateData, ThrowOnError>) => (options.client ?? client).put<DecksDecksUpdateResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/decks/decks/{id}/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List deck memberships (filter to one deck with ?deck=)
 *
 * A deck's membership feed + add/remove. A membership is a stateless join row, so this
 * is a plain List+Create+Destroy resource (NOT the imports ``@action``-chokepoint style,
 * which exists only for that app's batch/row state machine — decks have no such state).
 * OWNED-only is structural: the membership FKs ``CollectionItem``, so a non-owned card
 * has no id to add. Add is idempotent-aware — a duplicate ``(deck, collection_item)``
 * returns 409 (the holding is already in the deck), never a second row. Inherits
 * ``IsAuthenticated`` + ``PageNumberPagination``.
 */
export const decksMembershipsList = <ThrowOnError extends boolean = false>(options?: Options<DecksMembershipsListData, ThrowOnError>) => (options?.client ?? client).get<DecksMembershipsListResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/decks/memberships/',
    ...options
});

/**
 * Add an owned holding to a deck
 *
 * A deck's membership feed + add/remove. A membership is a stateless join row, so this
 * is a plain List+Create+Destroy resource (NOT the imports ``@action``-chokepoint style,
 * which exists only for that app's batch/row state machine — decks have no such state).
 * OWNED-only is structural: the membership FKs ``CollectionItem``, so a non-owned card
 * has no id to add. Add is idempotent-aware — a duplicate ``(deck, collection_item)``
 * returns 409 (the holding is already in the deck), never a second row. Inherits
 * ``IsAuthenticated`` + ``PageNumberPagination``.
 */
export const decksMembershipsCreate = <ThrowOnError extends boolean = false>(options: Options<DecksMembershipsCreateData, ThrowOnError>) => (options.client ?? client).post<DecksMembershipsCreateResponses, DecksMembershipsCreateErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/decks/memberships/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Remove a holding from a deck
 *
 * A deck's membership feed + add/remove. A membership is a stateless join row, so this
 * is a plain List+Create+Destroy resource (NOT the imports ``@action``-chokepoint style,
 * which exists only for that app's batch/row state machine — decks have no such state).
 * OWNED-only is structural: the membership FKs ``CollectionItem``, so a non-owned card
 * has no id to add. Add is idempotent-aware — a duplicate ``(deck, collection_item)``
 * returns 409 (the holding is already in the deck), never a second row. Inherits
 * ``IsAuthenticated`` + ``PageNumberPagination``.
 */
export const decksMembershipsDestroy = <ThrowOnError extends boolean = false>(options: Options<DecksMembershipsDestroyData, ThrowOnError>) => (options.client ?? client).delete<DecksMembershipsDestroyResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/decks/memberships/{id}/',
    ...options
});

/**
 * Service health
 *
 * Liveness probe — returns 200 when the process is up.
 */
export const healthRetrieve = <ThrowOnError extends boolean = false>(options?: Options<HealthRetrieveData, ThrowOnError>) => (options?.client ?? client).get<HealthRetrieveResponses, unknown, ThrowOnError>({ url: '/api/health/', ...options });

/**
 * List import batches with per-status row counts
 *
 * Import history + upload. List/retrieve batches with derived per-status row counts, and
 * POST a Dragon Shield CSV to ``/api/imports/batches/`` to run an import (slice 6). The review
 * surface acts on a batch's *rows* (``ImportRowViewSet``), never on batches directly. Defining
 * ``create`` makes the router bind POST on the collection route (no separate mixin needed).
 */
export const importsBatchesList = <ThrowOnError extends boolean = false>(options?: Options<ImportsBatchesListData, ThrowOnError>) => (options?.client ?? client).get<ImportsBatchesListResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/imports/batches/',
    ...options
});

/**
 * Upload a Dragon Shield CSV → run the import
 *
 * Upload a Dragon Shield CSV and run the import synchronously (DECISIONS 2026-05-29).
 *
 * Decode the file as utf-8-sig (mirroring the ``import_dragon_shield`` command — Excel
 * "CSV UTF-8" saves prepend a BOM), hand the text to ``run_import``, and return the created
 * batch with its derived row counts. A file that isn't a recognized DS export is **not** a
 * request error: ``run_import`` records a FAILED ``ImportBatch`` (a durable history row), so
 * this returns **201 with status=failed** and the frontend branches on ``batch.status``.
 * Only a missing / oversized / non-text file is a **400**. Synchronous (not Celery) is fine
 * at single-user few-thousand-row scale; ``MAX_UPLOAD_BYTES`` bounds the request time, and
 * ``run_import`` returns a JSON-native result so a future Celery promotion is a drop-in.
 */
export const importsBatchesCreate = <ThrowOnError extends boolean = false>(options: Options<ImportsBatchesCreateData, ThrowOnError>) => (options.client ?? client).post<ImportsBatchesCreateResponses, ImportsBatchesCreateErrors, ThrowOnError>({
    ...formDataBodySerializer,
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/imports/batches/',
    ...options,
    headers: {
        'Content-Type': null,
        ...options.headers
    }
});

/**
 * Retrieve one import batch
 *
 * Import history + upload. List/retrieve batches with derived per-status row counts, and
 * POST a Dragon Shield CSV to ``/api/imports/batches/`` to run an import (slice 6). The review
 * surface acts on a batch's *rows* (``ImportRowViewSet``), never on batches directly. Defining
 * ``create`` makes the router bind POST on the collection route (no separate mixin needed).
 */
export const importsBatchesRetrieve = <ThrowOnError extends boolean = false>(options: Options<ImportsBatchesRetrieveData, ThrowOnError>) => (options.client ?? client).get<ImportsBatchesRetrieveResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/imports/batches/{id}/',
    ...options
});

/**
 * List / filter import rows
 *
 * The review queue. List/filter staged rows (by ``batch`` / ``status`` /
 * ``match_confidence`` / ``needs_review`` — served by the ``(batch, status)`` index) and
 * resolve a PENDING one via three actions, all routed through ``apps.imports.sync`` so the
 * collection writes go through the single ``_materialize`` chokepoint (DECISIONS 2026-05-26
 * slice 4): ``approve`` materializes the row's matched printing, ``override`` re-points it at
 * a human-chosen printing, ``reject`` skips it.
 */
export const importsRowsList = <ThrowOnError extends boolean = false>(options?: Options<ImportsRowsListData, ThrowOnError>) => (options?.client ?? client).get<ImportsRowsListResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/imports/rows/',
    ...options
});

/**
 * Retrieve one import row
 *
 * The review queue. List/filter staged rows (by ``batch`` / ``status`` /
 * ``match_confidence`` / ``needs_review`` — served by the ``(batch, status)`` index) and
 * resolve a PENDING one via three actions, all routed through ``apps.imports.sync`` so the
 * collection writes go through the single ``_materialize`` chokepoint (DECISIONS 2026-05-26
 * slice 4): ``approve`` materializes the row's matched printing, ``override`` re-points it at
 * a human-chosen printing, ``reject`` skips it.
 */
export const importsRowsRetrieve = <ThrowOnError extends boolean = false>(options: Options<ImportsRowsRetrieveData, ThrowOnError>) => (options.client ?? client).get<ImportsRowsRetrieveResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/imports/rows/{id}/',
    ...options
});

/**
 * Approve a row → materialize its matched printing
 *
 * Materialize the row through ``_materialize``, overriding the auto-materialization
 * freshness gate (the reviewer is the human attention that gate proxies for — DECISIONS
 * 2026-05-27). 200 with the updated row on MATERIALIZED/SKIPPED; 409 when the holding was
 * already imported with a different quantity/cost/date (the row stays PENDING — the API
 * surfaces the conflict rather than overwriting cost basis); 400 if the row isn't an
 * approvable PENDING row with a matched printing.
 */
export const importsRowsApproveCreate = <ThrowOnError extends boolean = false>(options: Options<ImportsRowsApproveCreateData, ThrowOnError>) => (options.client ?? client).post<ImportsRowsApproveCreateResponses, ImportsRowsApproveCreateErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/imports/rows/{id}/approve/',
    ...options
});

/**
 * Override a row's matched printing
 *
 * Re-point the row at a human-chosen ``CardPrinting`` (body: ``{"printing": <id>}``)
 * and leave it PENDING so the corrected match can be approved next. Approvable
 * immediately after (``approve`` keys on a present matched printing, not on confidence).
 */
export const importsRowsOverrideCreate = <ThrowOnError extends boolean = false>(options: Options<ImportsRowsOverrideCreateData, ThrowOnError>) => (options.client ?? client).post<ImportsRowsOverrideCreateResponses, ImportsRowsOverrideCreateErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/imports/rows/{id}/override/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Reject a row → skipped
 *
 * Mark a PENDING row SKIPPED (the reviewer declines the holding). 400 if the row is
 * not PENDING (a MATERIALIZED row already wrote a holding; an ERROR row is terminal).
 */
export const importsRowsRejectCreate = <ThrowOnError extends boolean = false>(options: Options<ImportsRowsRejectCreateData, ThrowOnError>) => (options.client ?? client).post<ImportsRowsRejectCreateResponses, ImportsRowsRejectCreateErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/imports/rows/{id}/reject/',
    ...options
});

/**
 * List portfolios (with latest value snapshot inline)
 *
 * Read-only portfolios. Each row carries the latest ``PortfolioValueSnapshot``
 * inline (NULL when a portfolio has never been valued, e.g. a freshly-created
 * one from a Dragon Shield import that runs before the next 04:00 UTC
 * valuation beat — DECISIONS 2026-05-25 slice 4c).
 */
export const portfolioPortfoliosList = <ThrowOnError extends boolean = false>(options?: Options<PortfolioPortfoliosListData, ThrowOnError>) => (options?.client ?? client).get<PortfolioPortfoliosListResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/portfolio/portfolios/',
    ...options
});

/**
 * Retrieve one portfolio (with latest snapshot inline)
 *
 * Read-only portfolios. Each row carries the latest ``PortfolioValueSnapshot``
 * inline (NULL when a portfolio has never been valued, e.g. a freshly-created
 * one from a Dragon Shield import that runs before the next 04:00 UTC
 * valuation beat — DECISIONS 2026-05-25 slice 4c).
 */
export const portfolioPortfoliosRetrieve = <ThrowOnError extends boolean = false>(options: Options<PortfolioPortfoliosRetrieveData, ThrowOnError>) => (options.client ?? client).get<PortfolioPortfoliosRetrieveResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/portfolio/portfolios/{id}/',
    ...options
});

/**
 * List / filter portfolio value snapshots (append-only daily history)
 *
 * Read-only append-only daily valuation timeline. The slice-5 portfolio
 * chart consumes ``?portfolio=&from=&to=`` to pull a range, then renders the
 * value series. ``unrealized_gain`` may be NULL on a row (partial coverage —
 * DECISIONS 2026-05-25 slice 4a); consumers handle NULL distinctly from 0.
 */
export const portfolioSnapshotsList = <ThrowOnError extends boolean = false>(options?: Options<PortfolioSnapshotsListData, ThrowOnError>) => (options?.client ?? client).get<PortfolioSnapshotsListResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/portfolio/snapshots/',
    ...options
});

/**
 * Retrieve one portfolio value snapshot
 *
 * Read-only append-only daily valuation timeline. The slice-5 portfolio
 * chart consumes ``?portfolio=&from=&to=`` to pull a range, then renders the
 * value series. ``unrealized_gain`` may be NULL on a row (partial coverage —
 * DECISIONS 2026-05-25 slice 4a); consumers handle NULL distinctly from 0.
 */
export const portfolioSnapshotsRetrieve = <ThrowOnError extends boolean = false>(options: Options<PortfolioSnapshotsRetrieveData, ThrowOnError>) => (options.client ?? client).get<PortfolioSnapshotsRetrieveResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/portfolio/snapshots/{id}/',
    ...options
});

/**
 * List / filter price snapshots (append-only daily history)
 *
 * Read-only append-only price history. List filterable by
 * ``?printing=&edition=&from=&to=`` — the slice-4 price-chart shape. The
 * ``latest`` action returns the most-recent snapshot for one
 * ``(printing, edition)``, the "today's price" lookup; this is structured as
 * an action (not the latest-first ordered list's first row) so a consumer
 * can't accidentally page into history when it only wants today.
 */
export const pricingSnapshotsList = <ThrowOnError extends boolean = false>(options?: Options<PricingSnapshotsListData, ThrowOnError>) => (options?.client ?? client).get<PricingSnapshotsListResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/pricing/snapshots/',
    ...options
});

/**
 * Retrieve one price snapshot
 *
 * Read-only append-only price history. List filterable by
 * ``?printing=&edition=&from=&to=`` — the slice-4 price-chart shape. The
 * ``latest`` action returns the most-recent snapshot for one
 * ``(printing, edition)``, the "today's price" lookup; this is structured as
 * an action (not the latest-first ordered list's first row) so a consumer
 * can't accidentally page into history when it only wants today.
 */
export const pricingSnapshotsRetrieve = <ThrowOnError extends boolean = false>(options: Options<PricingSnapshotsRetrieveData, ThrowOnError>) => (options.client ?? client).get<PricingSnapshotsRetrieveResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/pricing/snapshots/{id}/',
    ...options
});

/**
 * Latest snapshot for one (printing, edition) — the 'today's price' lookup
 *
 * The most recent snapshot for one ``(printing, edition)``. 404 when no
 * snapshot exists for the pair (a printing TCGCSV doesn't price, or one
 * the daily reconcile hasn't run for yet).
 */
export const pricingSnapshotsLatestRetrieve = <ThrowOnError extends boolean = false>(options: Options<PricingSnapshotsLatestRetrieveData, ThrowOnError>) => (options.client ?? client).get<PricingSnapshotsLatestRetrieveResponses, PricingSnapshotsLatestRetrieveErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/pricing/snapshots/latest/',
    ...options
});

/**
 * Status dashboard — Healthchecks backup + CD checks
 *
 * Status dashboard — the Healthchecks tier (the flow's backup + CD dead-man nodes).
 *
 * Separate endpoint (not folded into overview) so a slow/down external provider
 * degrades this tile alone and never blocks the live internal flow. The provider
 * caches + fails closed to ``available: false`` (always 200, never 500). Optional:
 * ``configured: false`` without a read-API key.
 */
export const statusChecksRetrieve = <ThrowOnError extends boolean = false>(options?: Options<StatusChecksRetrieveData, ThrowOnError>) => (options?.client ?? client).get<StatusChecksRetrieveResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/status/checks/',
    ...options
});

/**
 * Status dashboard — internal pipeline + app health
 *
 * Status dashboard — the internal tier (the live pipeline flow + app state).
 *
 * Inherits the global ``IsAuthenticated``: the dashboard exposes operational internals
 * (run history, catalog size, deployed commit), not public data. A single typed dict
 * via ``@extend_schema`` (the ``HealthView`` shape), so the generated client gets a
 * typed ``statusOverviewRetrieve``. Pure DB reads, NOT cached — the heart of the page
 * must feel live; only the external provider tiers (Healthchecks/Hetzner) are cached.
 */
export const statusOverviewRetrieve = <ThrowOnError extends boolean = false>(options?: Options<StatusOverviewRetrieveData, ThrowOnError>) => (options?.client ?? client).get<StatusOverviewRetrieveResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/status/overview/',
    ...options
});

/**
 * Biggest price movers among owned (printing, edition) pairs
 *
 * Read-only "biggest movers" analytics (DECISIONS 2026-05-31): each owned
 * ``(printing, edition)``'s price change over a selectable window. Rows are
 * computed from the valuation engine's usable-price helpers across two date
 * anchors (today and today - window), scoped to currently-held holdings, then
 * server-ordered (the ``?ordering=`` allowlist) and paginated — like every other
 * list endpoint. Inherits the global ``IsAuthenticated`` + ``PageNumberPagination``.
 */
export const valuationMoversList = <ThrowOnError extends boolean = false>(options?: Options<ValuationMoversListData, ThrowOnError>) => (options?.client ?? client).get<ValuationMoversListResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'sessionid',
            type: 'apiKey'
        }],
    url: '/api/valuation/movers/',
    ...options
});
