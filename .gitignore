# ===== Python =====
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
.eggs/
build/
dist/
*.so

# ===== Virtual env =====
.venv/
venv/
env/
ENV/

# ===== Tooling caches =====
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.nox/
.coverage
.coverage.*
coverage.xml
htmlcov/
.hypothesis/

# ===== Django =====
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
media/
staticfiles/

# ===== Celery =====
celerybeat-schedule*
celerybeat.pid

# ===== Env files =====
.env
.env.*
!.env.example
# Hetzner self-host backup config — real secrets (the .example is committed)
backup.env
# Hetzner self-host deploy (pull-based CD) config — holds the Healthchecks ping URL
# (a credential); the .example is committed. Must stay ignored: the CD poller's
# `git clean -fd` would otherwise delete it on the first deploy (and it could be
# accidentally committed). deploy_poll.sh also fails closed if this isn't ignored.
deploy.env

# ===== Editor / IDE =====
.idea/
.vscode/
*.swp
*.swo

# ===== OS =====
.DS_Store
Thumbs.db

# ===== Local data samples and reconnaissance notes =====
# Raw scanner exports, pricing/metadata samples, and recon notes derived from
# them are kept local because real Dragon Shield exports + the notes that quote
# them contain personal collection data (folder names, cards owned, prices paid).
# Narrow paths only; `docs/` itself stays available for committed project docs.
data/samples/
docs/recon/
# Hetzner deploy runbook — kept local (public repo): it carries box-specific
# operational detail (the box's public IP, the SSH/admin-tunnel pattern, and
# co-tenant coexistence internals) not meant for a public repo. The committed,
# IP-clean config-as-code under infra/hetzner/ is the portfolio surface instead.
docs/hetzner-deploy-runbook.md

# ROADMAP.md is intentionally tracked and public in this repo (the milestone
# history for readers). A global git ignore may list ROADMAP.md as a personal
# cross-project default; this repo-local negation overrides it so the file
# stays committable.
!ROADMAP.md

# Note: keep uv.lock checked in for reproducible builds, do not ignore it.
